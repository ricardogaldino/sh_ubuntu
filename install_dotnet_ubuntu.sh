#!/bin/sh

# --------------------------------------------------

sudo apt update && sudo apt upgrade

# --------------------------------------------------

dotnet tool install --global dotnet-ef

# --------------------------------------------------

sudo apt update && sudo apt upgrade